<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="ListViewItem" x:Key="ChatItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border Background="White" CornerRadius="7,10,10,7" HorizontalAlignment="Left" BorderThickness="1" BorderBrush="gray" Margin="0,2,5,0" >
                        <StackPanel Orientation="Horizontal">
                            <StackPanel MaxWidth="{Binding ElementName=list_Messages, Path=ActualWidth}">
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="{Binding Sender}" FontWeight="SemiBold" Foreground="{DynamicResource MyBlue}"  VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Time}" Foreground="Gray" FontSize="12" VerticalAlignment="Center" Margin="0,0,5,2"/>
                                </StackPanel>

                                <TextBlock Text="{Binding Content}" FontWeight="SemiBold" TextWrapping="Wrap" VerticalAlignment="Center" Margin="5,0,5,2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>